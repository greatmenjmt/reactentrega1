import React from 'react';

const Contenedor = ({ mensaje }) => {
  return (
    <div className="contenedor">
      <h1>{mensaje}</h1>
      {/* catálogo */}
    </div>
  );
}

export default Contenedor;
